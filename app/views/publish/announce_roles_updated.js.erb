<% publish_to @channel do %>
  // Update roles list
  $('#roles-list-parent').empty().append("<%= j render(partial: 'shared/roles_list', locals: { roles: @roles, channel: @roles_list_channel, hide_admin_controls: true }) %>");
<% end %>